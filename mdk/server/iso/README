This CD can be used in different ways:
- as a bootable CD to install a new m23 server
  Insert the CD into a bootable CDRom drive and start the PC.
- to install m23 on a Debian Sarge server:
  Use the tool apt-cdrom:
    apt-cdrom -d <Mount point> add
  Replace <Mount point> with the mount directory of this CD.
  This could be e.g. /mnt/cdrom.
  Execute the following commands afterwards:
    apt-get update
    apt-get install m23

Additional information can be found at: http://m23.sf.net


Diese CD kann auf verschiedene Arten genutzt werden:
- als bootfähige CD, um einen neuen m23-Server zu installieren
  Legen Sie dazu die CD in ein bootfähiges CDRom-Laufwerk und starten
  Sie den PC.
- zum Installieren von m23 auf einem Debian-Sarge-Server:
  Benutzen Sie das Programm apt-cdrom:
    apt-cdrom -d <Mountpunkt> add
  Hierbei ist <Mountpunkt> das Verzeichnis, in das die CD eingehängt
  wird. Dies kann z.B. /mnt/cdrom sein.
  Führen Sie danach folgende Befehle aus:
    apt-get update
    apt-get install m23

Weitere Informationen finden Sie unter: http://m23.sf.net
